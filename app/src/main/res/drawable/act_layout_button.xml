<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@drawable/bg_gradientp" />
    <!-- Focused state -->
    <item android:state_focused="true" android:drawable="@drawable/bgg_gradient" />
    <!-- Default state -->
    <item android:drawable="@drawable/bgg_gradient" />
</selector>
